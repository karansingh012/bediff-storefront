interface FooterProps {
  className?: string;
}

export default function Footer({ className }: FooterProps) {
  // Store links, policies, and social navigation will go here.
  return <div className={className}>Footer</div>;
}
